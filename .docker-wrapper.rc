DOCKER_WRAPPER_IMAGE_node(){ echo "14.1.0-buster"; }

DOCKER_WRAPPER_SERVER_OPTS_npm(){ echo "-p ${LABO_PORT_PREFIX}${PUBLIC_PORT}:${PUBLIC_APP_PORT} -p ${LABO_PORT_PREFIX}${SECURE_PORT}:${SECURE_APP_PORT}"; }
